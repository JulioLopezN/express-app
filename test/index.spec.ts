import { describe } from 'mocha'
import { expect } from 'chai'

describe('RAD E template generator', function () {
    it('should 0 to be a number', async function () {

        expect(0).to.be.a('number')
    })
})
